node_modules/
dist/
*.log

# Code-signing material — never commit private keys
build/signing/keys/
*.p12
*.pfx
*.keystore
*.jks
*.key
!build/icons/*.png

# Stray key material from local testing. A literal "D:\a\_temp\..." filename
# once reached the repo and broke checkout on Windows runners.
*.crt
[A-Z]:\\*
graphene-keys/
